# Create a new release

1. Update `mod_version` in `gradle.properties`.
2. Commit that change.
3. Push that commit.
4. Create a matching git tag in the form `vX.Y.Z` e.g. `git tag v1.0.1`.
5. Push the tag with `git push origin v1.0.1`.